spring.application.name=cfi

# PORTA (Fly passa PORT) 👇
server.port=${PORT:8080}

# DATASOURCE (ideal: vir por secret no Fly)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://aws-1-sa-east-1.pooler.supabase.com:6543/postgres}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres.skngkvzaaooduznirjry}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:4LIj83YwoVOlHupc}

# Hikari - limitar conexões pra não estourar o Supabase
spring.datasource.hikari.maximum-pool-size=${DB_MAX_POOL:3}
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=10000
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.max-lifetime=30000

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.default_schema=public
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# uploads grandes (Tomcat)
spring.servlet.multipart.max-file-size=500MB
spring.servlet.multipart.max-request-size=2GB
spring.servlet.multipart.file-size-threshold=2MB
server.tomcat.max-swallow-size=-1
server.tomcat.max-http-form-post-size=-1

# cloudflare R2 (S3)
r2.account-id=2af935d3cb2ceb6bb414660ecbaa28af
r2.endpoint=https://${r2.account-id}.r2.cloudflarestorage.com
r2.bucket=cfi-media
r2.access-key=${R2_ACCESS_KEY}
r2.secret-key=${R2_SECRET_KEY}
r2.public-base-url=https://pub-5a15afe865fe44e9bf32a47b66b920c4.r2.dev

# logs úteis
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# JWT 👇
api.security.token.secret=${JWT_SECRET:my-secret-key}
